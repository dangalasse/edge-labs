{
  "name": "edge-labs",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — LLMOps error-log analysis via Google Gemini (AI Studio Free Tier)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.19.1"
  }
}
